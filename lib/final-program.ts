/**
 * 2027 미대입시 수능 파이널 집중반 — 계절성 설정 단일 소스.
 * /final 랜딩, 네비게이션 메뉴·상단 공지 띠, 사이트맵이 모두 이 객체만 참조한다.
 *
 * ── 반드시 지킬 것 ──────────────────────────────────────────
 * · 여기에는 확인된 정보만 둔다. 개강일·수업 시간·정원·수강료는 아직
 *   확정된 값이 코드에 없으므로 넣지 않는다 — 페이지에서는 "개별 상담 후
 *   안내"로 처리한다. 값이 확정되면 필드를 추가하고 페이지에서 꺼내 쓴다.
 * · "정시 캠프"·"정시 파이널 특강"이라는 표현은 쓰지 않는다. 미술학원에서
 *   정시 특강은 수능 뒤 실기 집중수업으로 오해되기 때문이다. 이 과정은
 *   9월 모의평가 이후부터 수능 전까지의 학과(국어·영어·사회탐구) 집중 과정이다.
 * ─────────────────────────────────────────────────────────────
 */

export const FINAL_PROGRAM = {
  /** 공식 명칭 — 제목·메타·JSON-LD에 그대로 쓴다 */
  name: "2027 미대입시 수능 파이널 집중반",
  /** 짧은 표기 — 헤더·메뉴·공지 띠처럼 폭이 좁은 자리용 */
  shortName: "2027 수능 파이널",
  /** 페이지 경로 */
  href: "/final",
  /** 대상 */
  target: "미대 정시를 준비하는 고3 · N수생",
  /** 상단 표기용 짧은 대상 문구 */
  targetShort: "2027 미대입시 · 고3/N수",
  /** 장소 */
  venue: "홍대 본원",
  venueName: "모두다른고양이 미술학원 홍대 본원",
  /** 2027학년도 대학수학능력시험일 (KST) */
  examDateISO: "2026-11-19",
  examDateLabel: "11월 19일 수능",
  /** 학과 과목 — 강사진(lib/teachers.ts)에서 확인되는 세 과목만 */
  subjects: ["국어", "영어", "사회탐구"],
} as const;

/**
 * 모집 노출 여부.
 * true  — 네비게이션 메뉴 "수능 파이널" 항목과 상단 공지 띠가 /final을 가리킨다
 * false — 메뉴·공지에서 빠지고 공지 띠는 윈터스쿨 안내로 돌아간다
 *         (/final 페이지 자체는 그대로 접근 가능)
 */
export const FINAL_IS_RECRUITING = true;

/** 상단 공지 띠 문구 — 짧게. 마감 임박·잔여 좌석 같은 자극 표현은 쓰지 않는다 */
export const FINAL_ANNOUNCEMENT_TEXT = `9평 이후, 수능까지 · ${FINAL_PROGRAM.shortName} 모집`;
